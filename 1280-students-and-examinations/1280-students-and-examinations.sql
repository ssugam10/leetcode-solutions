# Write your MySQL query statement below
select a.student_id, a.student_name, b.subject_name, (select count(*) from Examinations c where a.student_id = c.student_id and b.subject_name = c.subject_name) as attended_exams from Students a, Subjects b order by a.student_id, b.subject_name;